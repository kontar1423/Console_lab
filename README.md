# Console App Sample

Кроссплатформенное консольное приложение на Python, реализующее базовые Unix-команды с поддержкой macOS, Linux и Windows.

## 📋 Описание

Это демонстрационное приложение показывает современную архитектуру Python CLI-приложений с использованием:
- **Typer** - для создания CLI интерфейса
- **Dependency Injection** - для управления зависимостями
- **Platform-specific services** - автоматическое определение и использование правильной реализации для вашей ОС
- **Structured Logging** - логирование всех операций

## 🚀 Быстрый старт

### Требования

- Python >= 3.13
- [uv](https://github.com/astral-sh/uv) - современный менеджер пакетов Python

### Установка

1. **Клонируйте репозиторий** (если нужно):
   ```bash
   git clone <repository-url>
   cd Console-App-Sample
   ```

2. **Установите зависимости**:
   ```bash
   uv sync
   ```

3. **Установите приложение в editable mode**:
   ```bash
   uv pip install -e .
   ```

## 💻 Использование

### Основной способ (рекомендуется)

Используйте команду `app` через `uv run`:

```bash
uv run app <команда> [опции]
```

### Альтернативные способы

**Через Python модуль:**
```bash
uv run python -m src.main <команда> [опции]
```

**С активированным виртуальным окружением:**
```bash
source .venv/bin/activate
app <команда> [опции]
```

## 📚 Доступные команды

### `ls` - Список файлов и директорий

```bash
uv run app ls <путь>

# Примеры:
uv run app ls .
uv run app ls ~/Documents
uv run app ls test_dir
```

### `cat` - Чтение файла

```bash
uv run app cat <файл> [--bytes|-b]

# Примеры:
uv run app cat test_dir/hello.txt
uv run app cat test_dir/binary.dat --bytes
```

**Опции:**
- `--bytes` или `-b` - читать файл как байты (для бинарных файлов)

### `rm` - Удаление файлов и директорий

```bash
uv run app rm <путь> [--recursive|-r]

# Примеры:
uv run app rm file.txt
uv run app rm directory --recursive
uv run app rm -r old_folder
```

**Опции:**
- `--recursive` или `-r` - рекурсивное удаление директорий

### `mkdir` - Создание директории

```bash
uv run app mkdir <путь>

# Примеры:
uv run app mkdir new_folder
uv run app mkdir path/to/new/directory
```

### `touch` - Создание пустого файла

```bash
uv run app touch <файл>

# Примеры:
uv run app touch new_file.txt
uv run app touch path/to/file.txt
```

### `mv` - Перемещение/переименование

```bash
uv run app mv <источник> <назначение>

# Примеры:
uv run app mv old_name.txt new_name.txt
uv run app mv file.txt ~/Documents/
```

### `cp` - Копирование файлов и директорий

```bash
uv run app cp <источник> <назначение> [--recursive|-r]

# Примеры:
uv run app cp file.txt file_copy.txt
uv run app cp -r source_dir destination_dir
uv run app cp file.txt ~/Documents/  # Скопирует в директорию
```

**Опции:**
- `--recursive` или `-r` - рекурсивное копирование директорий

### `cd` - Изменение рабочей директории

```bash
uv run app cd <путь>

# Примеры:
uv run app cd ~/Documents
uv run app cd Projects/Console-App-Sample
```

**Примечание:** Текущая директория сохраняется между сессиями в `~/.console_app_state.json`

### Справка

Для получения помощи по любой команде:

```bash
uv run app --help
uv run app <команда> --help
```

## 🖥️ Поддержка платформ

Приложение автоматически определяет вашу операционную систему и использует соответствующую реализацию:

- **macOS** (`darwin`) → `MacOSConsoleService`
- **Linux** (`linux`) → `LinuxConsoleService`
- **Windows** (`win32`, `cygwin`) → `WindowsConsoleService`

### Особенности Windows

На Windows реализация включает дополнительную валидацию имен файлов:

- **Запрещенные символы**: `< > : " | ? * \`
- Автоматическая проверка при создании файлов/директорий (`touch`, `mkdir`, `cp`, `mv`)
- Информативные сообщения об ошибках с указанием найденных недопустимых символов

**Пример ошибки:**
```
Invalid filename: 'file?.txt' contains forbidden characters: ?
```

## 📁 Структура проекта

```
Console-App-Sample/
├── src/
│   ├── main.py              # CLI точка входа
│   ├── common/
│   │   └── config.py        # Конфигурация логирования
│   ├── dependencies/
│   │   └── container.py     # DI контейнер
│   ├── enums/
│   │   └── file_mode.py     # Enum для режимов чтения
│   └── services/
│       ├── __init__.py       # Фабрика для создания сервисов
│       ├── base.py           # Абстрактный базовый класс
│       ├── macos_console.py  # Реализация для macOS
│       ├── linux_console.py  # Реализация для Linux
│       ├── windows_console.py # Реализация для Windows
│       └── workspace_manager.py # Управление рабочим пространством
├── console_app.py           # Entry point wrapper
├── pyproject.toml           # Конфигурация проекта
├── uv.lock                  # Зафиксированные зависимости
└── README.md               # Этот файл
```

## 🗂️ Файлы и директории

- **`app.log`** - файл логов (ротация каждые 5MB, 5 backup файлов)
- **`~/.console_app_state.json`** - сохраненное состояние рабочей директории

## 🔧 Разработка

### Запуск тестов

```bash
# Проверка синтаксиса
uv run ruff check src/

# Форматирование кода
uv run ruff format src/
```

### Добавление новой команды

1. Добавьте абстрактный метод в `src/services/base.py`
2. Реализуйте метод во всех сервисах (macos, linux, windows)
3. Добавьте CLI команду в `src/main.py`

### Добавление поддержки новой платформы

1. Создайте новый класс в `src/services/`, наследующий `OSConsoleServiceBase`
2. Реализуйте все абстрактные методы
3. Добавьте условие в `src/services/__init__.py` → `create_console_service()`

## 📝 Примеры использования

### Работа с файлами

```bash
# Создать директорию и файл
uv run app mkdir my_project
uv run app touch my_project/readme.txt

# Записать в файл (через стандартный вывод)
echo "Hello World" | uv run app touch my_project/hello.txt

# Прочитать файл
uv run app cat my_project/readme.txt

# Скопировать файл
uv run app cp my_project/readme.txt my_project/readme_backup.txt

# Удалить файл
uv run app rm my_project/readme_backup.txt
```

### Работа с директориями

```bash
# Создать структуру директорий
uv run app mkdir -p project/src/modules

# Копировать директорию рекурсивно
uv run app cp -r project project_backup

# Удалить директорию
uv run app rm -r project_backup
```

### Использование с pipe и перенаправлением

```bash
# Подсчет файлов
uv run app ls . | wc -l

# Поиск по содержимому
uv run app cat file.txt | grep "pattern"

# Сохранение вывода
uv run app ls . > files_list.txt
```

## ⚠️ Важные замечания

1. **Рабочая директория**: Команда `cd` сохраняет состояние между запусками. Чтобы сбросить, удалите `~/.console_app_state.json`

2. **Пути**: Используйте абсолютные или относительные пути. Относительные пути разрешаются относительно сохраненной рабочей директории.

3. **Логирование**: Все операции логируются в `app.log` в корне проекта.

4. **Симлинки**: Приложение следует по симлинкам при проверке существования (`follow_symlinks=True`).

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект является демонстрационным примером и доступен для свободного использования.

## 🐛 Известные проблемы

- При первом запуске может потребоваться очистка `~/.console_app_state.json` если путь сохранен неправильно

## 📞 Поддержка

Если у вас возникли проблемы или вопросы, создайте issue в репозитории проекта.

---

**Версия**: 0.1.0  
**Python**: >= 3.13  
**Менеджер пакетов**: uv

